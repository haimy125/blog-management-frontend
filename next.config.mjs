/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["tailwindui.com"], // Thêm tên miền vào đây
    },
};

export default nextConfig;